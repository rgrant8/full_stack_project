<% if user_signed_in? %>    
    <%= link_to "Logout",destroy_user_session_path, :class => "btns btn-2", :"Logout" => :bar, :method => :delete;%>
<% end %>

<% unless user_signed_in? %>    
    <%= link_to "Login", new_user_session_path, :class => "btns btn-2", :method => :get %>
<% end %>
